name: Deploy Docker images
on: 
  push:
    branches: 
      - main 

jobs:
  docker-push:
    name: Docker Push
    runs-on: ubuntu-latest
    env:
      docker_username: ${{ secrets.DOCKER_USERNAME }}
      docker_password: ${{ secrets.DOCKER_PASSWORD }}
    steps:
      - name: Checkout 
        uses: actions/checkout@v2
      - name: Set commit variable
        run: export GIT_COMMIT="GA-$GITHUB_SHA"
      - name: Push Docker SHA
        run: chmod +x ci/deploy-docker-sha.sh && ci/deploy-docker-sha.sh
      - name: Push Docker latest
        run: chmod +x ci/deploy-docker-latest.sh && ci/deploy-docker-latest.sh